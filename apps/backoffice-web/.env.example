# Points the app at apps/backoffice-api. Leave unset locally — the client
# defaults to http://localhost:4001. Set for a fixed backend (staging).
# VITE_BACKOFFICE_API_URL="https://backoffice-api.agendya.co"
